package com.schoolfinder.domain

enum class Role { STUDENT, ADMIN }

enum class SchoolCategory { PRIMARY, SECONDARY, HIGH_SCHOOL, VOCATIONAL, UNIVERSITY }

enum class ReviewStatus { PENDING, APPROVED, REJECTED }
